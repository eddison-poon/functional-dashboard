# Query Engine

## Purpose

Provides a unified read-only interface for retrieving canonical repository objects.

## Example

```python
engine = QueryEngine(repository)
engine.scenarios()
engine.executions()
engine.total_executions()
```
