# Filters

Reusable predicate functions for QueryEngine.

## Available Filters

- filter_environment()
- filter_status()
- filter_priority()
- filter_feature()
- filter_requirement()
- filter_manual()
- filter_automation()

Use `compose()` to combine multiple predicates into a single reusable filter.
